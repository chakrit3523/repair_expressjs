<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        /* Reset some default styles */
        body, html, ul, li {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f9f9f9;
            color: #333;
        }

        /* Header styles */
        .header {
            background-color: #6A5ACD; /* สีม่วง */
            padding: 15px;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-radius: 10px;
            margin: 10px;
        }

        .header h1 {
            font-size: 20px;
            font-weight: bold;
        }

        .header nav ul {
            display: flex;
            list-style-type: none;
        }

        .header nav li {
            margin-left: 15px;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .header nav li:hover {
            color: #ddd;
        }

        /* Main content styles */
        .main-content {
            padding: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .card {
            background-color: white;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.1);
            padding: 15px;
            margin: 0;
            width: calc(25% - 20px); /* 4 cards per row */
            transition: transform 0.3s ease;
            border-radius: 10px;
            position: relative; /* For positioning the button */
        }

        @media (max-width: 768px) {
            .card {
                width: calc(100% - 20px); /* Full width on mobile */
                margin-bottom: 20px;
            }
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card h2 {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #6A5ACD; /* สีม่วง */
        }

        .card p {
            font-size: 14px;
            color: #666;
        }

        .card .btn {
            background-color: #6A5ACD; /* สีม่วง */
            border: none;
            color: white;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 14px;
            margin-top: 10px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            position: absolute;
            bottom: 10px;
            right: 10px;
        }

        .card .btn:hover {
            background-color: #5a4fbf;
        }

        /* Styles for mobile */
        @media (max-width: 768px) {
            .card .btn {
                font-size: 12px;
                padding: 5px 10px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Dashboard</h1>
        <nav>
            <ul>
                <li><a href="/home">Home</a></li>
                <li><a href="/profile">Profile</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </div>

    <div class="main-content">
        <% pages.forEach(function(page) { %>
            <div class="card">
                <h2><%= page.name %></h2>
                <p>นี่คือเนื้อหาสําหรับ <%= page.name %> อาจเป็นข้อมูลหรือข้อมูลประเภทใดก็ได้ที่เกี่ยวข้องกับแดชบอร์ดของคุณ</p>
                <a href="<%= page.path %>" class="btn">View More</a>
            </div>
        <% }); %>
    </div>
</body>
</html>
